mixin form-head()
	form.js-form-validate( data-parsley-validate="") 
		input(type='hidden', class="order", name='order', value=`Заявка  с сайта` ) 
		block
mixin input(  t , pl, n)
	+e.input-wrap.form-group
		+e.INPUT.input.form-control(type= t  placeholder= pl name= n)&attributes(attributes)
	// +e.input-wrap
mixin ta(p , pl)
	+e.input-wrap.form-group
		label
			+e.SPAN.title= p
			+e.TEXTAREA.input.form-control( placeholder= pl)&attributes(attributes)
	// +e.input-wrap

mixin form-wrap()
	+b.form-wrap&attributes(attributes)
		form
			block

mixin ci()
	+b.LABEL.custom-input
		+e.INPUT.input(type="checkbox")
		+e.SPAN.lab
		| 
mixin in-rad(n)
	+b.LABEL.custom-input
		+e.INPUT.input(type="radio" name= n)&attributes(attributes)
		+e.SPAN.lab
		block 
mixin in-ch()
	+b.LABEL.custom-input
		+e.INPUT.input(type="checkbox" name= n)&attributes(attributes)
		+e.SPAN.lab
		block 
mixin add-file(n)
	+b.add-file
		label
			+e.INPUT.input(name='file', type='file')
			+e.SPAN.btn 
			+e.SPAN.text   Прикрепить резюме
		+e.SPAN.filename 


mixin polite()
	+e.polite
		+in-ch('checked')(checked)
			+e.SPAN.text.text-gray Принимаю 
				| 
				a.text-body(href="#") условия передачи данных
				| 
				|  и&nbsp;согласен с&nbsp;
				a.text-body(href="#") политикой конфеденциальности.